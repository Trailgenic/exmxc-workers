{
  "version": "2.0",
  "dataset": "exmxc-entity-clarity-current",
  "description": "Current Entity Clarity Index observations measuring institutional posture, AI-facing capability, and Entity Clarity & Capability (ECC).",
  "methodology_id": "eci-ecc-v1",
  "fields": {
    "entity_id": {
      "type": "string",
      "description": "Stable entity identifier used across longitudinal snapshots."
    },
    "company": {
      "type": "string",
      "description": "Canonical name of the organization or institution."
    },
    "industry": {
      "type": "string"
    },
    "entity_type": {
      "type": "string"
    },
    "posture": {
      "type": "string",
      "allowed_values": [
        "Open",
        "Defensive",
        "Blocked"
      ]
    },
    "capability": {
      "type": "string",
      "allowed_values": [
        "Low",
        "Medium",
        "High"
      ]
    },
    "ecc": {
      "type": "integer",
      "range": {
        "min": 0,
        "max": 100
      },
      "description": "Entity Clarity & Capability score. Blocked observations are assigned 0 by definition."
    },
    "observed_at": {
      "type": "date",
      "format": "YYYY-MM-DD",
      "description": "Date of the observation."
    },
    "methodology_id": {
      "type": "string",
      "description": "Versioned scoring methodology identifier."
    }
  }
}
